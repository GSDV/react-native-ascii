# React Native ASCII

A React Native library for rendering ASCII/ANSI animations using an entity-component system. Built on React Native Skia and Reanimated.

This is a Yarn workspace. The ASCII package itself is in `packages/ascii`. An example app using the package is also in this repo.